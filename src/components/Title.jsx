import React from 'react'

function Title({content}) {
    return (
        <section className='section-title'>
            <h4>
            {content}
            </h4>
            <div/>
        </section>
    )
}

export default Title
